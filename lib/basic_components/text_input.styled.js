import styled from 'styled-components';

// Text Field: Single Line
export const TextField = styled.input.attrs({ type: 'text' })`

`;

// Text Area: Multi-Line
export const TextArea = styled.textarea.attrs({ type: 'text' })`

`;

// Search Field
export const SearchField = styled.input.attrs({ type: 'search' })`

`;

// Password Field
export const PasswordField = styled.input.attrs({ type: 'password' })`

`;